{
  "name": "almiftah-pipeline",
  "private": true,
  "type": "module",
  "description": "Builds the Al-Miftah static search index (Shia corpus mirror + semantic embeddings). Runs in GitHub Actions.",
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
